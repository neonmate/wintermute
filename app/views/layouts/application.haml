!!!
%html{lang: I18n.locale, class: 'h-100'}

  %head
    %meta(charset='utf-8')
    %meta(http-equiv='x-ua-compatible' content='ie=edge')
    %meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
    %meta(name='description' content='Wintermute should help programmers to create new web applications that can become more than just a side project. And help to explore already existing projects.')

    = render 'layouts/favicons'

    = csrf_meta_tags

    %title
      Wintermute

    = javascript_pack_tag 'application'
    = stylesheet_pack_tag 'application'

  %body{data: {env: Rails.env}, class: 'd-flex flex-column h-100'}
    .flex-shrink-0
      .container.mb-5
        = render 'layouts/navigation'
        = render 'layouts/flashes'

        .page-target
          .modal-target= yield
    = render 'layouts/footer'
